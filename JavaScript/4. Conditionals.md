## ৪. কন্ডিশন (Conditionals): ট্রাফিক লাইট

রাস্তায় ট্রাফিক পুলিশ যেমন গাড়ি থামায় বা যেতে দেয়, কোডেও আমরা শর্ত বা লজিক দিয়ে কন্ট্রোল করতে পারি। একে `if` এবং `else` বলা হয়।

- **যদি (if)** ট্রাফিক লাইট সবুজ হয় -> গাড়ি চালাও।
    
- **নাহলে (else)** -> গাড়ি থামাও।
    
### 💻 কোড উদাহরণ

```js
let trafficLight = "Red";

if (trafficLight === "Green") {
    console.log("Go now!"); // যদি সবুজ হয়, এটা প্রিন্ট হবে
} else if (trafficLight === "Red") {
    console.log("Stop immediately!"); // যদি লাল হয়, এটা প্রিন্ট হবে
} else {
    console.log("Get ready");
}
// আউটপুট: Stop immediately!
```

## 🛠️ Real-World Case Study: লগিন সিস্টেম

আসুন দেখি বাস্তবে ইঞ্জিনিয়াররা এই সহজ কনসেপ্টগুলো দিয়ে কীভাবে সমস্যার সমাধান করে।

**সমস্যা:** একজন ইউজার ফেসবুকে পাসওয়ার্ড দিয়ে লগিন করতে চাচ্ছে। **সমাধান:** আমাদের চেক করতে হবে পাসওয়ার্ড সঠিক কি না।

```js
// ফাংশন: লগিন চেক করার মেশিন
function checkLogin(inputPassword) {
    const realPassword = "Secret123"; // আসল পাসওয়ার্ড (const কারণ এটা ফিক্সড)
    
    // চেকিং (Condition)
    if (inputPassword === realPassword) {
        return "Welcome, User!"; // সাকসেস
    } else {
        return "Wrong Password!"; // ফেইল
    }
}

// ব্যবহার:
console.log(checkLogin("WrongPass")); // আউটপুট: Wrong Password!
console.log(checkLogin("Secret123")); // আউটপুট: Welcome, User!
```

## 🧪 Testing (নিজে যাচাই করুন)

নিচের কোডটুকু কপি করে আপনার ব্রাউজারের কনসোল (Console) এ পেস্ট করে এন্টার দিন। দেখুন ঠিকঠাক কাজ করে কি না।

```js
if (jogKoro(5, 5) === 10) {
    console.log("✅ অভিনন্দন! আপনি ফাংশন বুঝে গেছেন।");
} else {
    console.log("❌ কোথাও ভুল হয়েছে।");
}
```

___

## 📚 শব্দকোষ (Glossary) - সহজ বাংলা অর্থ

1. **Variable (ভেরিয়েবল):** বাক্স বা পাত্র, যেখানে তথ্য রাখা হয়।
    
2. **Value (ভ্যালু):** বাক্সের ভেতরের আসল জিনিসটা (যেমন: 10, "Dhaka")।
    
3. **Function (ফাংশন):** একটা ছোট মেশিন যা নির্দিষ্ট কাজ করে দেয়।
    
4. **Argument/Parameter:** মেশিনে যা ইনপুট দেওয়া হয়।
    
5. **Return:** মেশিন কাজ শেষে যা ফেরত দেয়।
    
6. **String (স্ট্রিং):** টেক্সট বা লেখা।
    
7. **Console.log:** জাভাস্ক্রিপ্টকে কোনো কিছু স্ক্রিনে দেখাতে বলার কমান্ড।
    
---

এখন কি বুঝতে একটু সুবিধা হচ্ছে? আপনি যদি এইটুকু বুঝে থাকেন, তাহলে আমরা পরের ধাপে **অ্যারে (Array)** বা তথ্যের তালিকা নিয়ে আলোচনা করতে পারি।

**Ready-to-use prompt:** `Explain: JavaScript Arrays`

Copyable snippet (একদম বেসিক কোড):

```js
// app.js
let name = "Developer";
const greeting = "Hello";

console.log(greeting + ", " + name + "!"); 
// Output: Hello, Developer!
```

