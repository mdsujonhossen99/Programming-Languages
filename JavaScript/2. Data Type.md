## ২. ডেটা টাইপ (Data Types): তথ্যের ধরন

কৌটায় (Variable) আমরা কী কী রাখতে পারি?

1. **String (লেখা):** যে কোনো টেক্সট। কোটেশন মার্ক `""` বা `''` এর ভেতরে যা থাকে। যেমন: `"Hello"`, `'Bangla'`.
    
2. **Number (সংখ্যা):** সাধারণ সংখ্যা। যেমন: `10`, `5.5`, `100`. (এখানে কোটেশন মার্ক লাগে না)।
    
3. **Boolean (সত্য/মিথ্যা):** সুইচ অন বা অফ করার মতো। শুধু `true` অথবা `false`।
    
### 💻 কোড উদাহরণ

```js
let fruitName = "Mango";  // String (লেখা)
let price = 120;          // Number (সংখ্যা)
let isTasty = true;       // Boolean (হ্যাঁ/না)

console.log(typeof fruitName); // আউটপুট: string
console.log(typeof price);     // আউটপুট: number
```

