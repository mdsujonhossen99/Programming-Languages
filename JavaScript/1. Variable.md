## ১. ভেরিয়েবল (Variables): তথ্য রাখার পাত্র

মনে করুন আপনার রান্নাঘরে মশলা রাখার ৩টি ভিন্ন ধরনের কৌটা বা জার আছে। প্রোগ্রামিংয়ে ভেরিয়েবল ঠিক এই কৌটার মতোই কাজ করে।

### 🟢 একদম সহজ ব্যাখ্যা

আমরা ৩টি শব্দ দিয়ে ভেরিয়েবল তৈরি করি: `let`, `const`, এবং `var`।

- **`const` (ফিক্সড কৌটা):** ধরুন আপনি একটি কৌটায় "লবণ" রাখলেন এবং সেটার গায়ে সুপার গ্লু দিয়ে "লবণ" লেবেল লাগিয়ে দিলেন। এর ভেতরের জিনিস আর পাল্টানো যাবে না। জাভাস্ক্রিপ্টে যে তথ্যের মান কখনো পরিবর্তন হবে না, তার জন্য `const` ব্যবহার করা হয়।
    
- **`let` (খোলা কৌটা):** এটি এমন একটি কৌটা যার ভেতরের জিনিস আপনি যখন খুশি পাল্টাতে পারেন। আজ "চিনি" রাখলেন, কাল সেটা ফেলে দিয়ে "ডাল" রাখলেন।
    
- **`var` (পুরানো ভাঙা কৌটা):** এটি অনেক আগের পদ্ধতি। এটি ব্যবহার করলে মাঝে মাঝে মশলা লিক হয়ে অন্য খাবারে মিশে যেতে পারে (কোডে বাগ তৈরি হয়)। তাই আমরা **`var` ব্যবহার করব না**।
    
### 💻 কোড উদাহরণ (রান করে দেখুন)

```js
// ১. const এর ব্যবহার (যা পাল্টানো যাবে না)
const myName = "Rahim";
console.log(myName); // আউটপুট: Rahim

// myName = "Karim"; // ❌ এটা করলে এরর খাবেন, কারণ const পাল্টানো যায় না।

const city = "Dhaka";
// city = "Chittagong"; // এই লাইন আনকমেন্ট করলে এরর দেবে, কারণ const বদলানো যায় না।

// ২. let এর ব্যবহার (যা পাল্টানো যাবে)
let pocketMoney = 50; 
console.log(pocketMoney); // আউটপুট: 50

pocketMoney = 100; // মান পরিবর্তন করলাম
console.log(pocketMoney); // আউটপুট: 100
```

